# Xcode
#
# gitignore contributors: paste build/ Industrial Strength / Template folder / Xcode.gitignore
build/
# Where `gh run download` puts MedxElite-unsigned.ipa. The IPA is a CI artifact and a release
# asset, never a tracked file — it is ~11 MB and rebuilt from source every push to main.
dist/
DerivedData/
*.moved-aside
*.xcuserstate
xcuserdata/
*.xcscmblueprint

## User settings
xcuserdata/

## Compatibility with Xcode 8 and earlier
*.xcclassmodel

## Swift Package Manager
.build/
Packages/
.swiftpm/xcode/xcuserdata/
# Where CI clones firebase-ios-sdk (`-clonedSourcePackagesDirPath`), and the workspace Xcode
# generates beside project.pbxproj the first time packages are resolved. Neither is authored:
# the pin lives in the XCRemoteSwiftPackageReference inside project.pbxproj.
.spm-cache/
MedxElite.xcodeproj/project.xcworkspace/

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon

# Agents & Skills
.agents/
skills-lock.json

